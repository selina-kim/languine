import { Text, View } from "react-native";

export default function Help() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>help page</Text>
    </View>
  );
}
